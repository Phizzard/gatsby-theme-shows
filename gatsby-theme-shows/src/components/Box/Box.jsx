import styled from "styled-components";
import { layout, space, color, flexbox, position } from "styled-system";

export default styled.div`
  box-sizing: border-box;
  min-width: 0px;
  ${position}
  ${flexbox}
  ${layout}
  ${space}
  ${color}
`;
